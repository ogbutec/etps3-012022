import 'package:flutter/material.dart';

class Configura extends StatelessWidget {
  const Configura({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      "Configuraciones",
      style: TextStyle(fontSize: 30),
    ));
  }
}
